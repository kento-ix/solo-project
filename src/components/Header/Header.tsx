import {
    Box,
} from "@mantine/core";
import classes from "./Header.module.scss";


const Header = () => {
    return(
        <Box pb={0}>
            <header className={classes.header}>
                
            </header>
        </Box>
    );
}

export default Header;